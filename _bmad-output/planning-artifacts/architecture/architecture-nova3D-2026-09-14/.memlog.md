---
scope: nova3D first-version system architecture, including approved post-PRD UX changes
purpose: implementation architecture; additional audience pending activation answer
altitude: initiative
updated: 2026-09-14T15:51
---

- (event) User selected Fast path on 2026-09-14: draft the full architecture with assumptions marked for review. User explicitly prefers a modern stack including Vercel, Supabase, and Upstash.
- (constraint) Input precedence: approved UX-SCOPE-CHANGES.md (2026-08-30) overrides conflicting PRD scope; final PRD and addendum (2026-08-27) govern remaining behavior; brief and brief addendum supply supporting direction. No product code or standing project-context.md exists; package.json currently contains only bmad-method.
- (constraint) Preserve all FR-1 through FR-30 and NFR-1 through NFR-12; controlled multi-account access, complete pre-generation research and Plan Approval, version-bound Model Approval, private workspaces, atomic paid-work permission and reservation, deletion tombstones and 30-day operational backup removal are adopted constraints.
- (constraint) Adopt UX SC-1 through SC-7: both picture workflows in first version, direct conversion offline on supported computers and phones, complete phone workflow, internationalization including RTL, phone push, live source exclusions, and WCAG 2.2 AA. Do not silently retain obsolete PRD picture deferrals.
- (constraint) PRD decision-register AD-1 through AD-7 are product ratification gates, not an existing parent architecture spine. Preserve them as explicitly namespaced PRD:AD-n references to avoid collision with new spine AD identifiers. Josh ratification before affected implementation remains required.
- (assumption) Additional audience question remains unanswered; proceed with implementation architecture only, retaining a readable decision and feasibility companion for reviewing fast-path assumptions. No separate stakeholder deck requested.
- (version) Verified 2026-09-14: Supabase official Next.js quickstart recommends create-next-app with with-supabase template, cookie-based auth, TypeScript and Tailwind: https://supabase.com/docs/guides/getting-started/quickstarts/nextjs . Starter is a seed, not adequate invitation-only authorization by itself.
- (version) Verified 2026-09-14: Vercel Functions generally available limits include 300s Hobby or 800s Pro/Enterprise, 2GB/4GB memory, 4.5MB payload; 1800s durations and 5GB bundles are beta: https://vercel.com/docs/functions/limitations . Avoid assuming long native CAD tasks fit without benchmark.
- (version) Verified 2026-09-14: Upstash Workflow durably orchestrates independent HTTP step executions; supports waiting/events and configurable retries: https://upstash.com/docs/workflow/basics/how and https://upstash.com/docs/workflow/features/retries . Nova3D must override automatic failure retries to preserve FR-6.
- (event) User confirmed Implementation architecture only; supersedes earlier pending audience assumption.
- (decision) AD-1 [ASSUMPTION]: Use a modular monolith with hexagonal boundaries, a Next.js application and isolated trusted CAD worker; dependencies point from UI/provider adapters to application use cases to pure domain contracts. This binds every feature and prevents provider SDKs or Temple-specific logic defining shared business rules. Prefer this over independently deployed microservices for a solo builder; runtime isolation does not create a second business authority.
- (decision) AD-2 [ASSUMPTION]: Supabase Postgres is the online source of truth. Each module owns its tables; invariant-changing commands use narrow transactional database functions, ownership-scoped relationships, optimistic concurrency and an outbox. Upstash/Redis and realtime signals do not own approvals, money or job truth. This prevents parallel data owners and partial state publication.
- (decision) AD-3 [ADOPTED]: Evidence-backed text and research-assisted picture workflows require complete plan and exact-version approval before geometry; image-derived conversion instead pins confirmed scope, image inputs and explicit uncertainty acknowledgment and never acquires historical-verification status. All qualified exports require approval of exact model content plus compatible validation. This prevents one branch bypassing the other branch's evidence requirements.
- (decision) AD-4 [ASSUMPTION]: Provenance is a typed immutable graph from source revision through claims, detail classifications, options, plan approval, typed parameters and feature identities to model manifests, model approvals, validation and exports. Structured source-record schema version 1 is the projection contract; PDF is derived. Stable semantic feature IDs are independent of mesh indices. This prevents orphaned and fabricated evidence.
- (decision) AD-5 [ADOPTED with ASSUMPTION mechanism]: Research freezes a finite six-dimension checklist and independent omission review, source eligibility epochs and passage-specific authority checks. Live exclusions invalidate affected draft conclusions and continue replacement research; completed records remain immutable. Agents receive untrusted source content as data and only a scoped tool allowlist. This prevents incomplete approval, stale source acceptance and prompt-based privilege escalation.
- (decision) AD-6 [ASSUMPTION]: Approved typed geometry recipes compile through versioned trusted domain generators into CadQuery solids in isolated native workers. Persist recipe, parameters, source-to-mm conversions, generator and dependency lock digests, BREP/STEP snapshot and feature manifest. Never execute arbitrary LLM Python. Cache immutable image-derived mesh snapshots separately; do not promise identical reruns of inference. Prefer local controllable CAD execution over a network-dependent KCL generation authority; benchmark before PRD:AD-1 ratification.
- (decision) AD-7 [ASSUMPTION]: Canonical units are millimetres in a right-handed Z-up frame; viewer GLB uses explicit millimetre-to-metre and Z-up-to-Y-up transforms. Preview LOD preserves semantic feature maps; measurements use canonical data; restore creates a new current pointer event without changing history; correction follows recorded dependency DAG. This prevents visually plausible scale errors and broken traceability after regeneration.
- (decision) AD-8 [ADOPTED with ASSUMPTION mechanism]: Print validation pins model content, exact target profile, physical scale, orientation and validator version. Repairs are immutable audited activities; consequential repair requires renewed approval, nonconsequential derivative requires verified equivalence. Permit one constrained full regeneration per immutable validation-attempt lineage, subject to existing paid-work permissions. Unsupported required checks block qualified export. This prevents stale approvals and unlimited recovery loops.
- (decision) AD-9 [ASSUMPTION]: Accept jobs only after committing job/outbox records. Upstash Workflow is replaceable orchestration using retries=0 for failed research/generation work; handle transport duplicates with durable step identities. A lost process lease becomes interrupted/failed, never a fresh paid execution. This preserves FR-6 manual retries and prevents undurable fire-and-forget execution.
- (decision) AD-10 [ASSUMPTION]: Workers must hold live leases/fencing tokens and commit against account authorization epoch, project deletion/operation epoch, input version and source-policy epoch. Cancellation/deletion revoke leases before best-effort provider cancellation; late results are discarded and private staging cleaned. This prevents stale jobs overwriting approved state or resurrecting deleted projects.
- (decision) AD-11 [ADOPTED with ASSUMPTION mechanism]: Reserve a hard upper cost bound in an atomic account-ledger transaction before each paid operation under project/category/provider/data disclosure authorization. Provider operation IDs and idempotency keys reconcile duplicate deliveries and unknown outcomes. Do not release unsettled possible charges on cancellation; budget resets preserve outstanding reservations. This prevents overspend and double billing.
- (decision) AD-12 [ADOPTED with ASSUMPTION mechanism]: Invitation redemption is single-use transactionally claimed, then idempotently provisioned through private Supabase admin auth API; public signup is off. Every request checks a live active account and non-revoked session grant, not JWT validity alone; admin has explicit control capabilities but no workspace-browsing privileges. Recovery and destructive controls require fresh authentication. This prevents reuse, expired-authority access and admin data leakage.
- (decision) AD-13 [ASSUMPTION]: Private artifacts use immutable account/project/version-scoped storage manifests, checksums, staged publication and per-request authorization. Do not use reusable download signed URLs to implement immediate revocation. Serve large files through a controlled streaming gateway with live authorization; use revocable, quota-limited staging uploads and verify before attachment. This prevents file capabilities outliving account disable or deletion.
- (decision) AD-14 [ADOPTED with ASSUMPTION mechanism]: Shared cache stores only immutable public-source-derived claims and provenance built via a strict promotion allowlist; account/project lookup and usage associations remain private. Gate promotion against tombstones and exclude private uploads, prompts and user decisions. Reuse is explicit, pinned and labelled with original date; changes create successor revisions. This prevents private-data retention through cache and silent evidence drift.
- (decision) AD-15 [ADOPTED with ASSUMPTION mechanism]: Tombstone first, revoke authorization and work, then purge active private data, derivatives, traces and operational backups within 30 days. Maintain a deletion ledger outside restore rollback and replay before restoring access; backup database and storage objects independently. Offline devices and third-party retention are explicit unresolved constraints, not a promise of remote instantaneous erasure. This prevents restoration resurrection and incomplete backup accounting.
- (decision) AD-16 [ASSUMPTION]: Offline direct-image work uses a prepared browser app, account-scoped IndexedDB metadata, OPFS artifact storage, verified cached model bundles, a dedicated inference worker and durable idempotent sync commands. Server authority never trusts local approvals or feature labels. Compare-and-set sync preserves divergent versions and respects deletion/disable before publication. Engine, device matrix and offline revocation policy remain release-blocking gates; ONNX is an evaluated candidate, not proven picture-to-3D capability.
- (decision) AD-17 [ADOPTED with ASSUMPTION mechanism]: Responsive full phone and computer UI uses language-tagged content, logical layout direction and structured source/translation/explanation fields; light default and optional dark theme. Evidence exploration and inspection expose equivalent keyboard/screen-reader controls and non-canvas feature lists. This prevents inaccessible canvas-only capability or conflating Hebrew original with translation.
- (decision) AD-18 [ASSUMPTION]: Transactional domain events create durable in-app notifications exactly once per event/recipient; optional web push is an at-least-once hint with opaque authenticated deep links and category preferences. No raw private subject/model data in push payload. UI source activity is recorded acquisition activity, not fabricated hidden reasoning. This prevents missing history or notification-based disclosure.
- (decision) AD-19 [ASSUMPTION]: One repository and primary data region with separate local/staging/production credentials, storage, queues and callback domains. Deploy reproducible worker images alongside compatible contract versions; previews use non-production resources and cannot issue production paid jobs. Observe lease age, failed jobs, unpaid settlements, purge deadlines and restore integrity with redacted telemetry. This prevents cross-environment callbacks, incompatible rollout and unobserved loss.
- (version) Selected seed registry versions checked 2026-09-14: next/create-next-app 16.3.5, react/react-dom 19.3.0, tailwindcss 4.3.3, @supabase/ssr 0.12.7 requiring @supabase/supabase-js ^2.114.0, supabase-js 2.116.0 requiring Node >=22, @upstash/workflow 1.3.3, @upstash/redis 1.38.4, three 0.186.0, cadquery 2.8.0 requiring Python >=3.11. Public npm package metadata and https://pypi.org/pypi/cadquery/json verified directly. TypeScript 5.9.3 exists; latest 7.0.2 was observed but not selected pending migration compatibility.
- (version) Node official release index https://nodejs.org/dist/index.json identifies v24.21.0 as current 24 LTS patch. Supabase with-supabase starter manifest inspected live: uses latest Next/Supabase, React19, but Tailwind3/TypeScript5 and eslint-config-next15.3.1. Recommend current create-next-app App Router TypeScript/Tailwind starter plus Supabase SSR integration to avoid silently inheriting mixed starter generations. https://nextjs.org/docs/app/getting-started/installation ; https://tailwindcss.com/docs/installation/framework-guides/nextjs
- (version) CadQuery import/export docs confirm Python recipe is the parametric representation, STEP is a snapshot, and 3MF plus assembly glTF exports are supported: https://cadquery.readthedocs.io/en/stable/importexport.html . Railway Docker build documentation verifies worker deployment path: https://docs.railway.com/builds/dockerfiles . Worker capacity and installed dependency closure remain unbenchmarked.
- (version) Supabase live-session checks needed because signed-out JWTs remain valid until expiry: https://supabase.com/docs/reference/javascript/auth-signout ; reusable signed download URLs are not application-revocable: https://supabase.com/docs/guides/storage/serving/downloads . Database backups do not include Storage objects: https://supabase.com/docs/guides/platform/backups .
- (version) ONNX Runtime supports cached browser inference but model/operator, memory and browser constraints do not prove acceptable multi-view 3D generation: https://onnxruntime.ai/docs/tutorials/web/ and https://onnxruntime.ai/docs/tutorials/web/large-models.html . Do not turn outdated WebGPU compatibility tables into support promises. iOS phone push requires Home Screen app setup and permission: https://webkit.org/blog/13878/web-push-for-web-apps-on-ios-and-ipados/ .
- (version) Adopted Bambu A1 mini fixture uses 0.4mm nozzle, silk PLA and 90mm maximum envelope; this 90mm is Josh's product constraint, not the printer's physical bed. Manufacturer lists 180mm build volume and 0.4mm included nozzle: https://us.store.bambulab.com/products/a1-mini . No nozzle-only derivation of wall/clearance thresholds is justified.
- (question) G-OFFLINE is a blocking product/feasibility gate: offline creation on supported phones has no selected proven model or measured device matrix. Remote immediate Account disable/deletion cannot revoke already downloaded data on disconnected devices. Josh must ratify the offline revocation/preparation/storage contract before that capability is built or advertised.
- (question) PRD:AD-1 through PRD:AD-7 remain unratified implementation gates: generation/toolchain proof, equivalence tolerance, print thresholds, full provenance schema, viewer targets, provider cost contracts, and source retention policy. A finalized architectural contract does not mark these experiments or ratifications complete.
- (decision) Input reconciliation clarification: evidence-assisted picture reconstruction is explicitly online-only; offline applies only to direct image-derived conversion. ERD uses ClaimSourceLink junction so a Claim may cite multiple Source Revisions without changing stable source/claim identity. OPFS, IndexedDB and WCAG 2.2 reference documentation also checked 2026-09-14.
- (decision) Reviewer rubric fixes bind adapter contracts: delivery duplicates always reconcile durable receipts even if transport retries; provider retry configuration requires an induced-failure check. Service requests bind signed environment/audience/payload/nonce with five-minute expiry and 60-second skew, key rotation and replay receipts. File gateway reauthorizes every at-most-1MiB chunk. CI/startup/callbacks mechanically reject mixed environments. Jobs pin contract/generator/worker image, dispatch only to registered compatible workers, and retain required old images for reproducibility.
- (decision) Independent consistency-review fixes: Artifacts exclusively owns canonical Postgres ArtifactManifest records and roots; storage pointers never redefine authority. Nonconsequential repairs bind a new artifact plus equivalence evidence to the exact approved canonical Model Version; consequential changes need successor approval. Research/cache adoption pins private effective exclusion-policy revisions in plan digests. Jobs owns Job/Attempt/Step/ExternalOperation identity; Usage ledger keys the operation; user retries create new attempts only after uncertain prior paid operations reconcile. Sync rejects disabled/tombstoned authority; clients purge learned deletion targets, lock on disable, and preserve G-8 as unresolved policy for disconnected uncertainty. Bootstrap verifies actual managed-plan/runtime/region limits.
- (decision) Technology review refined worker seed to Python 3.12 release line, with maintained patch and full OCP/container lock subject to G-1. Reviewer corrected initial Python<=3.12 claim after checking OCP 7.9.3.1 metadata (>=3.10,<3.15; newer wheels exist); documentation and wheel metadata differ, so the conservative baseline is an assumption, not a claimed upper compatibility bound.
- (event) Independent PRD/brief and UX reconciliation completed; all three required reviewer lenses ran and targeted fixes were verified. Rubric and technology pass. Consistency seams fixed; offline remote-revocation policy remains explicitly gated by G-8 and is not silently ratified. Architecture lint has zero findings; three Mermaid diagrams parsed using isolated Mermaid 11.17.2. No implementation/provider/geometry/device benchmarks were performed. Implementation architecture only; no extra human-facing rendering or external handoff requested.
- (event) spine finalized
- (event) User resumed workflow after exit; completed bmad-spec adoption into _bmad-output/specs/spec-nova3D/SPEC.md with original architecture AD IDs preserved as a read-only companion. Specification records 17 capabilities and a new unresolved direct-image recovery question (Q-11); no architecture assumption or implementation gate was silently ratified.
- (direction) Josh explicitly instructed 'ratify all fast path' on 2026-09-14. Adopt previously marked technical defaults and delegate closure of outstanding policy selections. Close A-1–A-3, Q-1–Q-11 and product ratification portions of G-1–G-9/PRD:AD-1–AD-7. Do not mark unrun engineering evidence as passed; qualified implementation/release acceptance remains evidence-gated.
- (decision) R-1 [ADOPTED] resolves Q-1 / A-1 / G-1 / PRD:AD-1: Adopt AD-1–AD-19, the modular monolith, current stack seed and CadQuery recipe/solid authority on Railway. Bootstrap locks the maintained Python 3.12 patch, OCP dependency closure and container digest and proves the altar/ramp plus personalization, cancellation and restore. Engineering may select compatible patches without another product approval; version existence is not installation or feasibility evidence.
- (decision) R-2 [ADOPTED] resolves Q-2 / A-3 / G-2 / PRD:AD-2: Adopt corresponding-feature topology checks, bidirectional surface distance ≤0.01 mm at final print scale, bounds delta ≤0.01 mm and relative volume delta ≤0.1%. Use a conservative distance upper bound: measured distance plus certified approximation/error bound must pass; inconclusive or tiny/zero-volume cases fail closed. Require exact feature identity, component count and closed-solid topology, and a fixed corpus including deliberate dimensional, thin-feature, hole, unit and scale failures. No physical printer accuracy is implied.
- (decision) R-3 [ADOPTED] resolves Q-3 / G-3 / PRD:AD-3: Adopt Bambu PLA Silk+ Gold, A1 mini 0.4 mm nozzle, ≤90 mm cube, 0.20 mm layers and three perimeters; inherit Bambu PLA Silk+ @BBL A1M manufacturer's profile and pin its full inheritance closure at qualification. Initial product validation thresholds: wall ≥1.2 mm, isolated feature ≥0.8 mm, mating clearance ≥0.4 mm, raised/recessed relief ≥0.6 mm. Unsupported overhang >45° from vertical or bridge >5 mm requires verified support/slicing analysis; unknown required checks block export. These are engineering acceptance values requiring fixture evidence, not manufacturer guarantees. Nonconsequential repairs are limited to normals/winding, duplicate or zero-area face removal and vertex welding only if R-2 proves topology/geometry unchanged; hole filling, remeshing, thickening and dimension changes are consequential.
- (decision) R-4 [ADOPTED] resolves Q-4 / G-4 / PRD:AD-4: Adopt provenance schema 1.0.0 expressed in JSON Schema Draft 2020-12, one shared contract generating TypeScript/Python validation, and Playwright 1.63.0 bundled Chromium PDF rendering in the container. Bundle Noto Sans and Noto Sans Hebrew with license files; pin font digests. Render escaped immutable evidence with no remote resources or script execution. Bilingual reading order, glyphs, reciprocal links, pagination and zero orphan links require fixture verification.
- (decision) R-5 [ADOPTED] resolves Q-5 / A-3 / G-5 / PRD:AD-5: Adopt current and previous stable desktop Chrome/Edge/Firefox, macOS/iOS Safari and Android Chrome. Qualification devices: MacBook Air M2 with 16 GB, Windows 11 Intel i5-1235U/Iris Xe laptop with 16 GB, iPhone 16 Pro and Pixel 9 Pro; these are test targets, not claims of owned or supported hardware. Freeze exact OS/browser builds in each report. Viewer maximum fixture 100,000 preview triangles, initial GLB ≤20 MB, ≤5 s interactive at 20 Mbps and p95 frame time ≤33 ms after load. Keep feature-map, accessibility and GPU-loss checks. For the 5 s target progressively load a coarse ≤5 MB first-view derivative; the 20 MB cap covers the full initial-detail set, not a single blocking download.
- (decision) R-6 [ADOPTED] resolves Q-6 / G-6 / PRD:AD-6: Select Anthropic Messages API claude-sonnet-5 for paid evidence synthesis/vision and Brave Web Search v1 for optional paid discovery; no paid third-party 3D provider. Use USD integer microdollars, default UTC-calendar-month limits $25/invitee and $50/Administrator, $5 research Job maximum cumulative across retries and $1 maximum per external operation. Each request preflights capped input/output tokens or bounded search-call count against a versioned rate snapshot, with atomic reservations; request-cost upper bounds must fit every remaining ceiling. Default permissions are off; research search and synthesis/vision are separate disclosed categories. Fixed managed hosting, storage, own-worker compute and local inference are instance overhead, outside personal paid-research allowance; free-only cannot call a metered AI/search endpoint even if it has promotional credits. Disable provider SDK automatic retries. Ambiguous calls hold their full reservation until matching usage evidence, conservative maximum settlement, or documented noncharge; no duplicate request is sent. Search sends normalized public subjects only, never private uploads, names, personalization or raw prompts. Anthropic receives only purpose-required selected content after data-category/retention disclosure; use stateless Messages, no Files/Batch/Managed Agents storage. Provider retention exceptions are disclosed and are outside controlled-backup deletion guarantees; unknown account terms block enablement. This ratifies product defaults, not live account spending or credentials.
- (decision) R-7 [ADOPTED] resolves Q-7 / G-7 / PRD:AD-7: Select Middot chapter 3 Hebrew Torat Emet 357 and English Mishnah Yomit by Dr. Joshua Kulp as the initial acquisition editions. Sefaria API metadata currently reports Public Domain and CC-BY respectively; capture exact metadata, attribution, source URL, license identifier/link, retrieved passage and digest before retaining bodies. Edition metadata must substantiate permitted retention and attribution; ambiguous/missing license details block body storage until verified. Public Domain, CC0 and verified CC-BY records with preserved attribution may enter the source allowlist; no generalized permission inferred from citation, public availability or search snippets. Unknown/restricted sources retain metadata and digest only and cannot substantiate approved claims without inspectable authorized evidence. Public approved revisions may persist while licensed; private associations follow deletion. Historical unit/interpretation choices still require each Project's Plan Approval.
- (decision) R-8 [ADOPTED] resolves Q-8 / A-2 / G-8: Adopt online preparation and online qualified export, plus revocation on reconnect for prepared offline direct conversion. Already-local private drafts remain usable while disconnected with no time lease; remote disable/deletion cannot erase them before contact. On contact, check authority before sync, lock on disable and purge on learned Account/Project deletion; explicit sign-out clears private stores. Network/auth uncertainty retains local work; do not unlock a previously learned disable without authoritative re-enable. Cloud access revokes immediately; the 30-day controlled-backup limit excludes disconnected devices and user-downloaded files. Make this limitation clear during offline preparation. Select ONNX Runtime Web 1.29.0 as first browser backend with supported WebGPU or WASM execution. Engineering is delegated model/weights selection, digest/license pinning and browser-compatible adaptation under unchanged multi-view/phone/offline quality scope; no model is claimed proven. Qualification targets ≤500 MiB preparation download, ≤1 GiB working memory and ≤120 s local conversion on R-5 devices, with held-out multi-view shape/coverage checks, eviction and interruption tests. A missing compliant engine remains release-blocking; it is not another permission question or authority to drop phone/multi-view requirements.
- (decision) R-9 [ADOPTED] resolves Q-9 / A-3 / G-9: Adopt US East primary layout: Vercel iad1, Supabase us-east-1 and Railway Virginia us-east4-eqdc4a; use nearest available Upstash primary and record actual topology. Select the smallest paid production tiers meeting required runtime, backup and recovery capabilities, with separate staging; engineering records exact plans/resources before provisioning acceptance. No private residency promise extends to global orchestration/AI/search. Active purge ≤24 h; controlled private backups ≤30 days after deletion; daily database and independent object backup, RPO ≤24 h and RTO ≤24 h, restore exclusion ledger replayed before access. Expiry must be measured from deletion, not extended by restoration/rebackup. Restore drills and actual plan settings remain unverified. This documentation work provisions no service.
- (decision) R-10 [ADOPTED] resolves Q-10 / UX: Select a calm light workspace with neutral surfaces, slate text, indigo primary actions, semantic evidence badges and an explicit dark option. Desktop uses a model viewport with evidence inspector; phones use stacked panels/drawers exposing every workflow control. Preserve RTL and WCAG 2.2 AA. Fast-path delegation supersedes the requirement to pause for alternative visual-direction selection or a specific external design-tool handoff. UX may complete the full authentication/admin/intake/research/jobs/model/correction/version/validation/export/settings responsive designs under this baseline; existing draft mockups are not retroactively marked produced, approved or tested.
- (decision) R-11 [ADOPTED] resolves Q-11 / G-3 / G-8: For direct-image failed local repair, allow at most one full reconversion in the same immutable validation lineage, using pinned ordered original images, confirmed scope, original validated inference bundle and explicit failed print constraints. Pin a new settings digest; if the engine cannot accept and satisfy the constraints, stop with an actionable failure. Create a successor Model Version, preserve the original and require inspection, exact-version Model Approval and full validation. Never invent a Research Plan for direct mode. A new validation record, worker retry or repair child cannot reset the consumed lineage allowance; ordinary failures still require user retry and all billable exceptions obey R-6. Evidence-backed regeneration retains exact-plan approval.
- (constraint) Ratification updates the architecture and derived SPEC/UX/readiness contracts with stable AD-1–AD-19 and CAP-1–CAP-17. Source precedence explicitly overrides earlier unratified/visual-checkpoint/offline-erasure/direct-recovery wording; preserve historical source records and add ratification notices to source artifacts. Remaining G gates are engineering verification, with no repeated Josh approval for these adopted defaults.
- (event) Primary documentation checked 2026-09-14 for new selections: Anthropic models/pricing/retention, Brave Web Search pricing and API privacy, official BambuStudio PLA Silk+ A1M profile, Sefaria v3 edition/license metadata, JSON Schema 2020-12, Playwright 1.63.0 and ONNX Runtime Web 1.29.0 npm metadata, Noto Hebrew fonts, and provider region docs. Technology availability is not measured app compatibility.
- (decision) Ratification review closure: AD-3 names evidence_text and evidence_images explicitly as the only complete-research/Plan-Approval modes; image_direct retains its separate confirmed-input gates. This clarifies wording without a scope change.
- (decision) R-6/AD-11 arithmetic: use checked integer USD microdollars and rational rates, never binary floats. One Usage-owned calculator sums all billable components under the operation's immutable rate/model/options snapshot, applies provider billing increments, and rounds upward once per operation to a microdollar; reservation uses the provable maximum of those components. Reject overflow, missing/foreign-currency rates and unsupported request parameters. All outstanding reservations, actual settlements and conservative maximum settlements count atomically against Account-period, parent-Job lifetime $5, and operation $1 limits; all attempts share that Job aggregate across resets. Conservative settlement retains its classification and maximum; later evidence appends a correcting ledger entry, never rewrites history. No new charge for reconciliation. G-6 tests concurrent attempt/admission, boundary rounding and ambiguous settlements.
- (decision) R-2/R-3 geometry comparison: compare canonical content in millimetres after the same pinned final-print scale/orientation transform; both sides use the original validation-lineage target-profile/transform revision, never independent best-fit/rescaling. Correspond corresponding semantic features under the same tessellator/settings and comparator/error-bound version; prohibit preview LOD as comparison authority. Volume delta denominator is the reference feature's absolute volume; zero/ill-conditioned feature volume is unknown/blocking. Nonconsequential equivalence proves canonical-to-repaired artifact under that same frame. G-2 includes rotations, unit scale and tiny-feature failures.
- (decision) R-8/AD-16 local state: permitted_local, locked_disabled and tombstoned. Fresh server-authorized Account and Project status is checked before each reconnect sync or qualified export; only a newer live authorization epoch for the original Account may transition locked_disabled to permitted_local. JWT refresh, successful connectivity or account switching cannot unlock it. Learned tombstones are terminal for their IDs and purge pending commands/files; older status responses cannot downgrade known disable/deletion revisions. Never interpret timeout as tombstone. Local status is not server authority.
- (decision) R-11/AD-8 bounded recovery: Manufacturing owns one immutable validation-lineage ID inherited by all repaired/regenerated versions, validations and Job attempts. A database-unique (lineage_id, full_regeneration) slot is atomically consumed with the successor Job/outbox and any initial cost reservation before dispatch; duplicate triggers return the same receipt. Once consumed, failure, cancellation, ambiguous charge, a new child/version/validation, or user retry cannot rearm it. User may explicitly start a separate modelling request with fresh inputs/scope/approvals, never implicitly reset the existing lineage. G-3/G-8 test simultaneous triggers and failed dispatch/retry.
- (decision) R-5 viewer timing: measure open-to-interactive from the user's model-open action, including transfer/decoding of required viewer code and the first-view derivative on a cold model cache at shaped 20 Mbps. First view means working orbit plus selected-feature/evidence navigation; load <=5 MB coarse geometry within the adopted 5-second total budget and full initial details <=20 MB progressively. Both derivatives carry version-compatible semantic feature maps; switch atomically without losing selection/provenance. The geometry budgets alone do not prove the latency target. G-5 measures code/network/decode/render together.
- (event) Ratification reviewer gate PASS after logged clarifications: independent PRD and UX reconciliation, rubric, technology and adversarial consistency reviews completed. Targeted consistency closure confirms all four P1 and progressive-view P2 resolved. Technology reviewer confirms immutable request/options/rates and accounting close model-parameter concern. Reviews/ratification-disposition.md records triage; engineering checks remain NOT RUN.
- (event) Self-validation Pass 1 (coherence): PASS. Stable 17 capabilities with intent/success and five kernel fields; all former A-1–A-3/Q-1–Q-11 product questions have adopted resolutions, no unsupported verification claims. Five companions include architecture-owned ratified decisions; kernel has no diagrams.
- (event) Self-validation Pass 2 (preservation): PASS. All 30 FRs, 12 NFRs, metrics and glossary retained by PRD; all seven SCs/detailed UX requirements retained. Ratification changes to CAP-12/CAP-14/CAP-16, offline authority and visual delegation are explicit and traced. Source PRD/UX notices identify precedence; historical memlog questions and draft mockups remain history, not current blockers or approved artifacts.
- (event) Mechanical verification PASS: zero spine lint findings; stable AD-1–AD-19, CAP-1–CAP-17 and R-1–R-11; complete FR/SC mappings; five companion/four source paths and local links resolve; no template comments, kernel diagrams or trailing whitespace. Three previously parsed architecture Mermaid diagrams unchanged. Product ratification complete; G-1–G-9 engineering acceptance/release evidence NOT RUN.
- (event) spine finalized; fast-path ratification recorded and synchronized on 2026-09-14; engineering verification remains unrun
- (event) Josh requested execution of feasibility, device, print-validation and recovery checks using MCP Omnigent Luna sub-agents on 2026-09-14. Four isolated Codex-native sessions with requested model gpt-5.6-luna/high are launched for CAD/stack, browser/offline, manufacturing and local Postgres/object recovery probes. Existing dirty source records remain authorized prior work. Qualification scripts and evidence are separate from unbuilt product implementation; no real target device, deployed provider, physical printer or paid API qualification is inferred.
- (event) Qualification evidence from the September 14 Omnigent MCP Luna run is recorded at _bmad-output/implementation-artifacts/qualification-2026-09-14/REPORT.md. Local exact web-stack production builds, synthetic native CAD, headless browser/storage/identity-runtime, print fixtures plus official BambuStudio slice, and local Postgres/object recovery probes passed within their stated scope. G-1/G-2/G-3/G-6/G-9 remain PARTIAL; G-5/G-8 BLOCKED; G-4/G-7 NOT RUN. No application release gate is passed, target hardware qualified, offline reconstruction engine selected, provider provisioned, or product guarantee weakened. This event supersedes blanket engineering-NOT-RUN wording while preserving AD/CAP/R IDs.
- (event) Qualification review: 28 independent Luna findings triaged; 26 addressed with probe/evidence corrections, two pre-existing application gaps deferred. Final parent reruns passed for exact stack production build/HTTP, native synthetic CAD (including actual generated-envelope negatives), browser byte persistence/runtime, print fixtures and local Postgres/object recovery. Software slicing passed in the pinned isolated BambuStudio environment. Application gates remain open per REPORT.md.
- (event) Self-validation Pass 1 (coherence): PASS. The five-field kernel, 17 CAP IDs and 19 AD IDs are unchanged; current gate statuses link to scoped evidence and preserve ratified requirements.
- (event) Self-validation Pass 2 (preservation): PASS. No product, geometry, approval, privacy, offline or UX acceptance obligation was removed. Historical NOT RUN entries remain in append-only logs; current source notices and readiness companions identify the subsequent local checks. Real devices, certified comparator, complete validator, offline reconstruction, deployed retention and cross-store crash recovery remain unqualified.
- (event) Qualification run completed: scoped local evidence, reproducible probes, independent review disposition and final parent checks are retained in implementation-artifacts/qualification-2026-09-14. Spine lint has zero findings; AD-1–AD-19, CAP-1–CAP-17 and R-1–R-11 remain stable. Current local links and retained artifact manifest verify. All full application gates remain open under their documented PARTIAL/BLOCKED/NOT RUN states.
