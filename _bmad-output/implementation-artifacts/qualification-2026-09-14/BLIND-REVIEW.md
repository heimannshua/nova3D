- `tools/qualification/feasibility/cadquery_probe.py:153-161` validates only the base X/Y dimensions and the sum of three vertical dimensions; it does not validate the actual generated bounds or the translated ramp/plaque extents. A parameterized feature can therefore extend beyond the envelope while the parameter gate reports no envelope error.
- `tools/qualification/feasibility/cadquery_probe.py:302-331` labels negative cases “fail closed” without ever passing them through a rejecting build/validation boundary. It only checks that an error list is non-empty, and the hole case only compares coarse volume, so these assertions do not demonstrate rejection behavior.
- The scale and unit negative cases in `tools/qualification/feasibility/cadquery_probe.py:309-317` are confounded by the envelope check: multiplying every dimension by 2 or 25.4 predictably creates oversized geometry. They do not test unit interpretation, scale normalization, or a dedicated scale/unit error, so the reported PASS can survive a broken unit/scale implementation.
- `tools/qualification/feasibility/cadquery_probe.py:217` treats any non-zero child exit as successful cancellation. It does not require the expected SIGTERM exit, verify that termination was caused by the probe, or detect a worker crash after the readiness marker; a crash could be reported as PASS.
- The deterministic-generation assertion in `tools/qualification/feasibility/cadquery_probe.py:255-266` compares only validity, solid/topology counts, bounds, and volume. It records recipe digests and feature metrics but never compares them, and it retains no exact generated artifact hash, so materially different geometry with the same coarse metrics can pass.
- The correction check compares individual feature shapes in `tools/qualification/feasibility/cadquery_probe.py:296-330` but never compares the corrected final union against the original final union or checks boolean interaction boundaries. A correction can alter the assembled result while all selected per-feature comparisons still pass.
- The geometry corpus has no negative for an out-of-envelope translated feature/ramp, disconnected or merely touching solids, invalid numeric values (`NaN`/infinity), or a malformed operation graph. Those cases would exercise the missing bounds and fail-closed paths more directly than the current oversized-base cases.
- `tools/qualification/print/qualify_fixtures.py:147-160` infers wall, isolated-feature, clearance, and relief values from components sorted by X position and fixed indexes. It does not assert five uniquely identified components or derive the semantics from fixture metadata, so an extra/reordered component can produce incorrect measurements or an index error rather than a controlled failure.
- `tools/qualification/print/qualify_fixtures.py:195-202` intentionally bypasses `mesh_gate` for `positive_envelope` and `positive_thresholds`; their `expected_match` depends only on the analytic gate. A malformed positive mesh whose bounds and positional thresholds look valid could therefore be reported as an expected match despite failing watertightness or zero-area checks.
- The print probe runs the slicer only on the single positive 40 mm cube described in `_bmad-output/implementation-artifacts/qualification-2026-09-14/print/REPORT.md:96`. No negative/malformed/threshold/overhang/bridge fixture is sliced, so the retained slice cannot detect whether the effective process rejects or mishandles invalid geometry or support-sensitive cases.
- `tools/qualification/stack/setup-stack.sh:14` uses `npm install` rather than a lockfile-enforced install. Although `stack/package-lock.json` is retained as evidence, the setup script neither copies nor consumes it, leaving transitive dependency resolution mutable on a later reproduction.
- `.../stack/source-input-sha256.txt` hashes only `package.json`, `tsconfig.json`, `postcss.config.mjs`, and `setup-stack.sh`; it omits every checked-in `app/` source file and the minimal fixture. The build/runtime evidence therefore has no integrity binding to the source that the report says was exercised.
- The stack report repeats the import, TypeScript, and Tailwind rows (`qualification-report.md:53-62`) and does not retain the actual HTTP response bodies in `clean-production-curl.log` (the file is empty). The report's claims about marker presence and route JSON are consequently harder to independently verify than the build logs.
- The device offline assertion in `tools/qualification/devices/probe.mjs:187-191` checks only that the fetched artifact has length 33 and that a cache entry exists. It never compares the fetched bytes with the cached bytes or a digest, so a stale/wrong same-length artifact can pass the offline check.
- The device offline pass deliberately omits Three.js and ONNX assets (`probe.mjs:159-162`), and the Service Worker caches only `/index.html` and `/artifact.bin` (`fixture/sw.js:1-3`). Thus the PASS in `PARENT-CHECKS.json` covers storage and a tiny shell fixture, not offline viewer or inference startup; the aggregate should expose this as incomplete rather than a plain area PASS.
- The “phone layout” contexts in `tools/qualification/devices/probe.mjs:212-216` execute the same API/geometry fixture but collect no DOM dimensions, overflow checks, screenshots, interactions, or frame/latency measurements. They provide no evidence for layout acceptance despite being named phone layout emulations.
- `tools/qualification/recovery/probe.py:378-387` tests stale-callback fencing only after the deletion transaction has deleted `job-delete-001` and its project. `UPDATE 0` is therefore explained by the missing row/project, not by rejecting a stale callback against a retained tombstone or an existing cancelled job; the claimed fencing behavior is vacuous.
- `tools/qualification/recovery/probe.py:273-276` calls the ledger durable after `fsync` on the file, but never fsyncs the containing directory or tests recovery after a process/power-loss window. The durable-intent claim is stronger than the retained failure injection demonstrates.
- The recovery deletion sequence has no failure injection between DB commit and object unlink, between object unlink and ledger status update, or during replay. It does not prove that an interrupted deletion is discoverable and safely resumable, which is the central cross-store recovery risk.
- `tools/qualification/recovery/probe.py:330-376` restores the same dump/snapshot it just created, records their hashes, but never verifies the dump hash before `pg_restore` or the snapshot hash before extraction. It checks only the survivor object afterward, leaving corruption/tampering of the backup inputs largely untested.
- The recovery import test at `tools/qualification/recovery/probe.py:418-425` repeats the same command ID sequentially. It does not race two distinct command IDs sharing a local request ID, nor race the same command concurrently, so neither the declared local-request uniqueness nor the atomic idempotency path is qualified.
- `tools/qualification/recovery/probe.py:108-141` reserves against an account and job independently: `usage_jobs` has no account foreign key and `reserve_operation` never verifies account ownership or account status. There are no disabled/tombstoned-account or cross-account-job negatives, allowing an important authorization/billing invariant to remain untested.
- The recovery duplicate-reservation check (`probe.py:426-431`) is sequential and returns `duplicate` without checking that a retry with the same idempotency key has identical amount, account, job, and operation identity. A conflicting replay payload could therefore be accepted as an idempotent duplicate.
- `tools/qualification/recovery/probe.py:548-551` records the regeneration-slot check as PASS based only on the substring `slot_count=1` and the import check based only on `import_count=1`; it ignores the computed `slot_pass`, `import_pass`, and changed-payload rejection result. Those assertions can report PASS when one of the concurrent sessions or payload-integrity checks fails.
- `tools/qualification/recovery/probe.py:223-225` ignores failures from `docker rm -f`, while the report states that the probe performs cleanup. The evidence does not verify that the container was actually removed, and a failed cleanup can leave state behind for subsequent runs.
- `tools/qualification/feasibility/CadQuery.Dockerfile:11-22` leaves the Debian runtime packages and the `pip/setuptools/wheel` upgrade unpinned. The base image digest is pinned, but a later rebuild can receive different native libraries or packaging tools, weakening the claimed exact toolchain reproducibility.
- The image-to-3D candidate review in `devices/qualification-report.md:67-84` cites mutable repository/model-card URLs without commit IDs, retrieval timestamps, archived content, or response hashes. The negative licensing, size, and hardware conclusions cannot be independently reproduced from the retained artifact alone.
- Source-integrity evidence is uneven: unlike the device probe, the feasibility, print, and recovery artifacts retain no manifest binding their probe scripts and generated fixtures to the reported runs. A later rerun can silently use changed local scripts while the old JSON/report remains labelled as the result.
