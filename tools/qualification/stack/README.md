# nova3D stack compatibility probe

This is a minimal scratch Next App Router project. It verifies the ratified
dependency versions can be installed, imported, typechecked, built, and served
locally. It is framework compatibility evidence only: it does not qualify
authentication, jobs, persistence, providers, or deployed application code.
